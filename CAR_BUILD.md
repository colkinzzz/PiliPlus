# PiliPlus 车机适配版

本分支为 Android 车机与分屏窗口增加以下行为：

- 播放器“全屏”只改变 PiliPlus 当前窗口内的布局，不请求改变宿主窗口或横竖屏；只有原生检测可靠确认车机已经整屏时才进入沉浸模式。
- 车机分屏或窗口状态未知时始终保留系统栏，避免系统层拦截可见控件的触控。
- 使用透明状态栏的 edge-to-edge 绘制，让主题背景延伸到车机状态条区域；主页交互内容只消费一次顶部安全区。
- 设置 → 样式设置中增加“车机窗口模式”和顶部/底部最低安全边距；车机专用 Action 产物默认启用。
- 专用 APK 使用 `com.example.piliplus.car` 包名和 `PiliPlus Car` 名称，可与官方 PiliPlus 同时安装。

车机窗口、播放器布局和系统栏是三个相互独立的状态：宿主为分屏/整屏/未知，播放器为普通/应用内全屏，系统栏为显示/沉浸。分屏播放器全屏固定使用 contain，保留必要黑边，避免裁切。

窗口检测只把 DisplayManager 枚举到的 `TYPE_BUILT_IN` 显示屏作为物理基准；`TYPE_VIRTUAL`、overlay 或 Wi-Fi 显示屏的 Display.Mode 不可信，无法唯一匹配真实物理屏时按未知状态处理。

## 构建

运行 GitHub Actions 中的 **Build Car Android arm64-v8a**。工作流只构建 `arm64-v8a`（V8A）APK，并上传为 `PiliPlus-Car-arm64-v8a` artifact。

如果车机没有正确上报系统栏高度，可在“设置 → 样式设置”中逐级调大顶部或底部最低安全边距；修改后重启应用。

## 验收矩阵

1. 车机右侧 2/3 分屏：普通页面控件不进入顶部状态栏；播放器全屏后仍保持分屏、系统栏可见、视频完整。
2. 车机系统整屏：播放器全屏后才隐藏上下系统栏；退出播放器全屏恢复系统栏，但不改变宿主窗口。
3. 窗口状态无法确认：播放器仍可在当前窗口内全屏，但不隐藏系统栏、不请求方向切换。
4. 手机/非车机：沿用原有方向与沉浸行为。
